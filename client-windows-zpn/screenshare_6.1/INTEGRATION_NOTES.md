# 项目整合说明（第一阶段）

本版本按“保留 zpn UI，复用 wyd 底层链路”的方式整合，未直接使用 wyd 的界面。

## 已完成

1. 保留原有主会议界面：大屏 + 5 个小屏 + 底部控制栏。
2. 点击“开始共享”后弹出卡片式共享内容选择页：桌面 1、桌面 2、白板、已打开窗口 / 最小化窗口。
3. 保留 zpn 白板与画笔逻辑：关闭画笔不清空笔迹，结束共享才释放批注层。
4. 共享选择页底部增加选项：共享音频、显示鼠标指针、流畅模式。
5. 接入本地媒体链路：
   - 麦克风采集：`AudioCapturer`
   - 系统声音采集：`SystemAudioCapturer`（WASAPI loopback）
   - 音频混音：`AudioMixer`
   - 本地发送打包：`Sender + DebugTransport`
6. 视频帧、混音音频、批注事件都会进入 `Sender`，当前只输出 Debug 日志，为第二阶段网络传输预留接口。

## 第一阶段范围

当前阶段重点是本地整合，不做真正远端网络会议。`Sender` 负责统一打包，`DebugTransport` 只是打印发送日志。

## 第二阶段建议

1. 将 `DebugTransport` 替换为真实传输层，例如 TCP / UDP / WebRTC。
2. 增加 Receiver：解包视频、音频、批注事件并渲染远端画面。
3. 视频编码可先 JPEG，后续升级 H.264；音频可先 PCM，后续升级 Opus。
4. 发送协议可继续沿用 `sender.h` 里的 `PacketHeader`。

## 构建提示

需要 Qt 6.5+，并需要 Qt Multimedia 模块。Windows 下系统声音采集会链接 `ole32 / mmdevapi / avrt`。


## 2026-06-01 更新

- 保留并接入类似 WYD 版本的共享悬浮工具条：暂停/继续共享、批注开关、麦克风静音、共享音开关、本地回放、回到主窗口、结束共享。
- 共享选择页中的“桌面1 / 桌面2”卡片已改为实时桌面缩略图，不再使用原来的电脑图标；未检测到第二屏时仍隐藏桌面2。
- 工具条 UI 可保留，其他主界面与共享选择页仍保持 zpn 风格。

## v3 更新：摄像头 + 本机双开 TCP 通信

本版在 v2 的基础上继续保留 zpn 主 UI 和共享选择页，只新增最小必要控件：

- `开启监听`：作为主机监听 9000 端口。
- `连接本机`：连接 `127.0.0.1:9000`，用于同一台电脑双开两个 exe 测试。
- `打开摄像头 / 关闭摄像头`：打开本机摄像头，画面显示在上方第一个小窗口，不占用大窗口。

推荐测试流程：

1. 打开第一个 exe，点击 `开启监听`。
2. 打开第二个 exe，点击 `连接本机`。
3. 第一个 exe 点击 `开始共享` 并选择桌面/窗口/白板。
4. 第二个 exe 点击 `打开摄像头`。
5. 预期效果：
   - 第二个 exe 的大窗口显示第一个 exe 传来的共享画面。
   - 第一个 exe 的上方小窗口显示第二个 exe 传来的摄像头画面。

当前网络层使用 `TcpPacketTransport`，包格式为：长度前缀 + Sender 生成的媒体包。视频第一版使用 JPEG 编码；音频包和批注包已经保留传输入口，但远端音频播放和远端批注渲染留到下一阶段继续完善。

## v3 fix2

- 修复摄像头远端不显示：开启摄像头时显式启用 `Sender` 的 `CameraPip` 视频流，关闭摄像头时再禁用。
- 原因：`Sender` 默认把 `CameraPip` 注册为 disabled，导致本机小窗口能预览摄像头，但不会真正进入网络发送队列。

## v4_multithread_perf 更新

本版主要解决双开测试时 UI 卡顿：

- 新增 `VideoEncodeWorker`：JPEG 编码放到独立线程，主线程不再直接做 `image.save("JPEG")`。
- 新增 `VideoDecodeWorker`：远端 JPEG 解码放到独立线程，避免接收端 UI 卡住。
- 视频帧采用“只保留最新帧”的策略：如果上一帧还没编码/解码完成，新来的同一路视频会覆盖旧帧，避免延迟越积越高。
- 桌面共享发送分辨率默认限制到 1280×720；流畅模式限制到 960×540。
- 摄像头小窗发送分辨率限制到 480×270，摄像头采集回调节流到约 10fps。
- TCP 写缓冲超过约 4MB 时，非可靠视频包会被丢弃，优先保证实时性。

说明：`QScreen::grabWindow()` / `QPixmap` 相关采集仍保留在 GUI 线程，这是 Qt 的安全边界；本版重点把最重的 JPEG 编码和解码移出 GUI 线程。

## v4_multithread_perf 更新

这版针对双开共享 + 摄像头卡顿做了性能拆分：

- 新增 `VideoEncodeWorker`：JPEG 编码移动到独立 `QThread`，主线程不再直接执行 `image.save(..., "JPEG")`。
- 新增 `VideoDecodeWorker`：远端 JPEG 解码移动到独立 `QThread`，避免接收端 UI 因 `loadFromData()` 卡顿。
- Sender 视频流改成“只保留最新帧”：如果上一帧还在编码，新的同类视频帧会覆盖旧待处理帧，防止延迟越积越多。
- Receiver 视频解码也改成“只保留最新帧”：远端画面慢时自动丢旧帧，优先保证实时性。
- 桌面共享发送分辨率限制：普通模式约 1280x720，流畅模式约 960x540。
- 摄像头发送分辨率限制：约 480x270。
- 摄像头采集端增加帧率节流，避免双开时 30fps 持续转 QImage。
- TCP 发送侧增加 socket 写缓冲保护：如果网络缓冲堆积过多，非可靠视频帧直接丢弃，避免画面延迟持续变大。

当前仍保留 Qt `QScreen::grabWindow()` / GDI `PrintWindow()` 的本地采集方式，采集本身仍由 UI 定时器触发；本版主要先拆掉最重的 JPEG 编解码和视频积压问题。后续如果还需要继续优化，可以再做真正的采集线程或改 H.264/Opus。
