# v8.3 UI status area and toolbar text update

## 改动内容

1. 底部状态栏改为单独一整行显示，避免“已创建会议、会议码、端口”等信息被按钮挤压截断。
2. 底部按钮下移到第二行并居中排列，保持原来的蓝/绿/红/灰配色。
3. 悬浮工具条中的“批注:开/关”统一改为“画笔:开/关”，避免和底部“画笔”按钮出现两个不同名称。

## 说明

底部“画笔”按钮和共享时出现的悬浮工具条“画笔:开/关”属于同一个功能入口：

- 底部按钮适合在主窗口中操作；
- 悬浮工具条适合共享过程中快速操作。

因此保留两个入口，但统一命名为“画笔”。
